# ✅ МНОЖЕСТВЕННЫЕ ТЕМАТИКИ (ДО 5) - РЕАЛИЗОВАНО!

## 🎨 Что сделано:

### **1. Обновлена схема базы данных**

#### **Добавлена таблица `slot_themes` (many-to-many)**

```prisma
model slot_themes {
  id         String   @id @default(cuid())
  slot_id    String
  theme_id   String
  created_at DateTime @default(now())
  slots      slots    @relation(fields: [slot_id], references: [id], onDelete: Cascade)
  themes     themes   @relation(fields: [theme_id], references: [id], onDelete: Cascade)

  @@unique([slot_id, theme_id])
  @@index([slot_id], map: "idx_slot_themes_slot_id")
  @@index([theme_id], map: "idx_slot_themes_theme_id")
}
```

#### **Обновлена модель `slots`**

- Старое поле `theme_id` переименовано в `theme_id_deprecated` для обратной совместимости
- Добавлена связь `slot_themes slot_themes[]` для множественных тематик

#### **Обновлена модель `themes`**

- Добавлена связь `slot_themes slot_themes[]` для связи с слотами

---

### **2. Обновлён Backend**

#### **slots-prisma.service.ts**

Обновлён метод `findBySlug` для включения `slot_themes`:

```typescript
async findBySlug(slug: string): Promise<Slot | null> {
  return this.prisma.slots.findUnique({
    where: { slug },
    include: {
      providers: true,
      slot_categories: true,
      slot_mechanics: {
        include: {
          mechanics: true,
        },
      },
      slot_bonuses: {
        include: {
          bonuses: true,
        },
      },
      slot_themes: {  // ✅ Новое!
        include: {
          themes: true,
        },
      },
    },
  });
}
```

#### **slots.service.ts**

**Метод `createSlot`:** Добавлено создание связей в `slot_themes` (до 5):

```typescript
// Добавляем тематики (до 5)
if (selected_themes && selected_themes.length > 0) {
  console.log("🔧 Добавление тематик:", selected_themes);

  const themesToSave = selected_themes.slice(0, 5); // Ограничиваем до 5
  const themesData = themesToSave.map((themeId) => ({
    slot_id: slot.id,
    theme_id: themeId,
  }));

  await this.prisma.slot_themes.createMany({
    data: themesData,
  });

  console.log("✅ Тематики добавлены:", themesToSave.length);
}
```

**Метод `updateSlot`:** Обновлена логика обработки тематик:

```typescript
// Обрабатываем тематики (до 5)
if (selected_themes !== undefined) {
  console.log("🔧 Обновление тематик:", selected_themes);

  // Удаляем старые связи
  await this.prisma.slot_themes.deleteMany({
    where: { slot_id: id },
  });

  // Создаём новые связи (максимум 5)
  if (selected_themes && selected_themes.length > 0) {
    const themesToSave = selected_themes.slice(0, 5); // Ограничиваем до 5
    const themesData = themesToSave.map((themeId) => ({
      slot_id: id,
      theme_id: themeId,
    }));

    await this.prisma.slot_themes.createMany({
      data: themesData,
    });

    console.log("✅ Тематики обновлены:", themesToSave.length);
  }
}
```

---

### **3. Обновлена Admin панель**

#### **Изменён тип input с `radio` на `checkbox`**

**Было:**

```vue
<input
  v-model="selectedThemes[0]"
  :value="theme.id"
  type="radio"
  name="theme"
/>
```

**Стало:**

```vue
<input
  v-model="selectedThemes"
  :value="theme.id"
  type="checkbox"
  :disabled="selectedThemes.length >= 5 && !selectedThemes.includes(theme.id)"
  class="w-4 h-4 text-emerald-600 bg-gray-600 border-gray-500 rounded focus:ring-emerald-500 focus:ring-2"
/>
```

#### **Добавлено ограничение до 5 тематик**

- `selectedThemes.length >= 5` блокирует выбор дополнительных тематик
- Отображается счётчик: `Выбранные тематики (3/5)`
- Появляется предупреждение при достижении лимита: `⚠️ Достигнут максимум (5 тематик)`

#### **Обновлена загрузка данных**

**Было:**

```javascript
if (slot.value?.themes) {
  selectedThemes.value = [slot.value.themes.id];
}
```

**Стало:**

```javascript
if (slot.value?.slot_themes && Array.isArray(slot.value.slot_themes)) {
  selectedThemes.value = slot.value.slot_themes.map((st) => st.theme_id);
}
```

---

### **4. Обновлена клиентская часть**

#### **Hero секция**

**Было:** Отображение одной тематики из `slot.themes`

```vue
<span v-if="slot?.themes">
  {{ slot.themes.icon }} {{ slot.themes.name }}
</span>
```

**Стало:** Отображение множественных тематик из `slot.slot_themes`

```vue
<template v-if="slot?.slot_themes && slot.slot_themes.length > 0">
  <span
    v-for="slotTheme in slot.slot_themes"
    :key="slotTheme.id"
    class="px-3 py-1 bg-yellow-500/30 text-white rounded-full text-xs font-medium border border-yellow-400/20"
  >
    {{ slotTheme.themes?.icon }} {{ slotTheme.themes?.name }}
  </span>
</template>
```

---

## 🚀 Как использовать:

### **Шаг 1: Открой админку**

```
http://localhost:3000/admin/slots/gate-of-olympus
```

### **Шаг 2: Найди секцию "Тематика Hero секции"**

Прокрути вниз до секции **"Hero секции"**, найди подсекцию **"Тематика Hero секции"**.

### **Шаг 3: Нажми "Show"**

Разверни секцию, нажав кнопку **"Show"**.

### **Шаг 4: Выбери до 5 тематик**

Отметь чекбоксы нужных тематик (например):

- ☑️ Mythology
- ☑️ Adventure
- ☑️ Ancient Greece
- ☑️ Fantasy
- ☑️ Epic

Когда выбрано 5 тематик, остальные чекбоксы станут неактивными (`:disabled`).

### **Шаг 5: Проверь счётчик**

Внизу секции появится:

```
Выбранные тематики (5/5):

[Mythology] [Adventure] [Ancient Greece] [Fantasy] [Epic]

⚠️ Достигнут максимум (5 тематик)
```

### **Шаг 6: Сохрани**

Нажми **"Сохранить изменения"** внизу страницы.

В консоли backend должны появиться логи:

```
🔧 Обновление тематик: [ 'uuid-1', 'uuid-2', 'uuid-3', 'uuid-4', 'uuid-5' ]
✅ Тематики обновлены: 5
✅ Слот полностью обновлен со связями: { themes_count: 5 }
```

### **Шаг 7: Проверь на клиенте**

1. Открой страницу слота:

   ```
   http://localhost:3000/slots/gate-of-olympus
   ```

2. **Обнови страницу** (Ctrl+F5)

3. **Прокрути до Hero секции**

4. В блоке **"Themes"** должны отображаться все 5 выбранных тематик:

   ```
   🏛️ Themes

   [⚡ Mythology] [🗺️ Adventure] [🏛️ Ancient Greece] [🧙 Fantasy] [⚔️ Epic]
   ```

---

## 🎓 Объяснение (метафора):

### **Tinder профиль** 💕

#### **Было (1 интерес):**

**Профиль:**

```
Имя: Иван
Интерес: Спорт
```

❌ **Проблема:** Слишком мало информации! Другие пользователи не понимают, кто ты.

#### **Стало (до 5 интересов):**

**Профиль:**

```
Имя: Иван
Интересы:
  • Спорт 🏋️
  • Путешествия ✈️
  • Кино 🎬
  • Программирование 💻
  • Горы 🏔️
```

✅ **Результат:** Намного больше информации! Другие пользователи лучше понимают, кто ты, и больше шансов на match!

**Так же и со слотом:**

- **Было:** 1 тематика (например, "Mythology")
- **Стало:** До 5 тематик (например, "Mythology", "Adventure", "Ancient Greece", "Fantasy", "Epic")
- **Результат:** Пользователи лучше понимают, о чём слот, и больше шансов, что они будут играть!

---

## 📊 Структура данных:

### **API ответ (было):**

```json
{
  "id": "123",
  "name": "Gates of Olympus",
  "theme_id": "uuid-theme-1",
  "themes": {
    "id": "uuid-theme-1",
    "name": "Mythology",
    "icon": "⚡"
  }
}
```

### **API ответ (стало):**

```json
{
  "id": "123",
  "name": "Gates of Olympus",
  "theme_id": null,
  "slot_themes": [
    {
      "id": "uuid-1",
      "slot_id": "123",
      "theme_id": "uuid-theme-1",
      "themes": {
        "id": "uuid-theme-1",
        "name": "Mythology",
        "icon": "⚡"
      }
    },
    {
      "id": "uuid-2",
      "slot_id": "123",
      "theme_id": "uuid-theme-2",
      "themes": {
        "id": "uuid-theme-2",
        "name": "Adventure",
        "icon": "🗺️"
      }
    },
    {
      "id": "uuid-3",
      "slot_id": "123",
      "theme_id": "uuid-theme-3",
      "themes": {
        "id": "uuid-theme-3",
        "name": "Ancient Greece",
        "icon": "🏛️"
      }
    }
  ]
}
```

---

## 🔍 Диагностика:

### **Проверка базы данных:**

Создай скрипт `backend/check-themes.js`:

```javascript
const { PrismaClient } = require("@prisma/client");
const prisma = new PrismaClient();

async function checkThemes() {
  const slot = await prisma.slots.findFirst({
    where: { slug: "gate-of-olympus" },
    include: {
      slot_themes: {
        include: {
          themes: true,
        },
      },
    },
  });

  console.log("Тематики слота:", slot.slot_themes);
  console.log("Количество тематик:", slot.slot_themes?.length || 0);

  await prisma.$disconnect();
}

checkThemes();
```

Запусти:

```bash
cd backend
node check-themes.js
```

**Ожидаемый результат:**

```
Тематики слота: [
  {
    id: 'uuid-1',
    slot_id: '123',
    theme_id: 'uuid-theme-1',
    themes: { id: 'uuid-theme-1', name: 'Mythology', icon: '⚡' }
  },
  {
    id: 'uuid-2',
    slot_id: '123',
    theme_id: 'uuid-theme-2',
    themes: { id: 'uuid-theme-2', name: 'Adventure', icon: '🗺️' }
  }
]
Количество тематик: 2
```

---

## 📋 Контрольный список:

- [ ] Backend запущен
- [ ] Админка открыта (`http://localhost:3000/admin/slots/...`)
- [ ] Секция "Тематика Hero секции" развёрнута
- [ ] Выбрано до 5 тематик (чекбоксы отмечены)
- [ ] Счётчик отображает: `Выбранные тематики (X/5)`
- [ ] Нажата кнопка "Сохранить изменения"
- [ ] В консоли backend появились логи "✅ Тематики обновлены"
- [ ] Страница слота обновлена (Ctrl+F5)
- [ ] В Hero секции отображаются все выбранные тематики

---

## 🎯 Итог:

### ✅ **Теперь можно выбрать до 5 тематик для слота!**

### ✅ **Автоматическое ограничение - невозможно выбрать больше 5**

### ✅ **Визуальный счётчик и предупреждение**

### ✅ **Отображение всех тематик в Hero секции на клиенте**

---

**Готово!** 🚀✨

Теперь у слотов может быть до 5 тематик вместо одной!
