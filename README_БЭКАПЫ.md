# ⚡ БЫСТРЫЕ КОМАНДЫ ДЛЯ БЭКАПА И ВОССТАНОВЛЕНИЯ

## 🎯 ИЗ КОРНЯ ПРОЕКТА

```bash
# Создать полный бэкап (БД + JSON)
npm run backup

# Создать быстрый бэкап (только БД)
npm run backup:quick

# Восстановить из последнего бэкапа
npm run restore

# Проверить данные в базе
npm run db:check
```

## 📁 ИЗ ПАПКИ BACKEND

```bash
cd backend

# Все команды бэкапа
npm run db:backup           # Быстрый (1 сек)
npm run db:auto-backup      # Полный (5 сек)
npm run db:restore          # Восстановление (2 сек)
npm run db:check            # Проверка данных
```

---

## 🔥 БЫСТРЫЙ СТАРТ

### Сценарий 1: Обнулилась база данных

```bash
# Из корня проекта:
npm run restore

# ИЛИ из backend:
cd backend
npm run db:restore

# Готово! Все данные восстановлены за 2 секунды!
```

### Сценарий 2: Перед внесением изменений

```bash
# Быстрый бэкап перед экспериментами:
npm run backup:quick

# Вносите изменения...

# Если что-то сломалось:
npm run restore
```

### Сценарий 3: В конце рабочего дня

```bash
# Полный бэкап всех данных:
npm run backup

# Отправить в Git:
git add backend/studio-data/
git commit -m "Daily backup $(date +%Y-%m-%d)"
git push
```

---

## ✅ ЧТО У ВАС СЕЙЧАС

### Сохранено в 2 местах:

**1. Файл базы данных (.db):**

- `backups/db/dev_backup_LATEST.db` (308 KB)
- Используется для мгновенного восстановления

**2. JSON файлы:**

- `backend/studio-data/providers.json` (34 KB, 94 записи)
- `backend/studio-data/mechanics.json` (2.87 KB, 6 записей)
- `backend/studio-data/themes.json` (70.37 KB, 241 запись)
- `backend/studio-data/slot_categories.json` (8.58 KB, 31 запись)
- `backend/studio-data/slots.json` (29.73 KB, 5 записей)

---

## 📊 ТЕКУЩИЕ ДАННЫЕ

После восстановления у вас будет:

- ✅ **94 провайдера** игр
- ✅ **6 игровых механик**
- ✅ **241 тема** слотов
- ✅ **31 категория** слотов
- ✅ **5 тестовых слотов**

**Общий размер: 308 KB** (очень компактно!)

---

## 💡 РЕКОМЕНДУЕМЫЙ WORKFLOW

```bash
# Утром
npm run db:check            # Проверить данные

# Перед изменениями
npm run backup:quick        # Быстрый бэкап (1 сек)

# В конце дня
npm run backup              # Полный бэкап (5 сек)
git add backend/studio-data/
git commit -m "Daily backup"
git push

# Если что-то сломалось
npm run restore             # Восстановление (2 сек)
```

---

## 🎓 АНАЛОГИЯ

**Представьте Git как систему сохранений в игре:**

- **`npm run backup`** = Создать точку сохранения
- **`npm run restore`** = Загрузить сохранение
- **JSON в Git** = Облачное сохранение (доступно на любом компе)

Если "погибли" в игре (обнулилась БД) — загружаетесь с последней точки сохранения за 2 секунды!

---

## ⚠️ ВАЖНО

1. **Перед восстановлением остановите backend** (Ctrl+C)
2. **JSON файлы коммитьте в Git** — они текстовые и легкие
3. **.db файлы НЕ коммитятся** (в .gitignore, слишком большие)

---

## 📞 БЫСТРАЯ СПРАВКА

| Команда (из корня)     | Время | Что делает         |
| ---------------------- | ----- | ------------------ |
| `npm run backup:quick` | 1 сек | Копия .db файла    |
| `npm run backup`       | 5 сек | .db + JSON экспорт |
| `npm run restore`      | 2 сек | Восстановить всё   |
| `npm run db:check`     | 1 сек | Проверить данные   |

---

✨ **Теперь у вас есть молниеносное решение для сохранения и восстановления данных!** ✨

📁 **Полная инструкция:** `БЭКАП_ИНСТРУКЦИЯ.md`
📁 **Детали:** `ГОТОВО_БЭКАПЫ.md`
