# 🚀 ЗАПУСК ПРОЕКТА SLOTQUEST

## ✅ ВСЕ ДАННЫЕ ВОССТАНОВЛЕНЫ!

База данных содержит:

- ✅ 94 провайдера
- ✅ 6 механик
- ✅ 241 тему
- ✅ 31 категорию
- ✅ 5 слотов

Данные находятся в: `backend/dev.db`

---

## 🚀 КАК ЗАПУСТИТЬ ПРОЕКТ

Все команды запускаются **ИЗ КОРНЯ ПРОЕКТА** (папка `SlotQuest`):

### 1. Запустить Backend

Откройте Git Bash в **корне проекта** и выполните:

```bash
npm run backend
```

**Дождитесь сообщения:**

```
🚀 Backend server running on http://localhost:3001
📊 API available at http://localhost:3001/api
```

### 2. Запустить Frontend

Откройте **НОВЫЙ** терминал Git Bash в **корне проекта** и выполните:

```bash
npm run frontend
```

**Дождитесь сообщения:**

```
✔ Client compiled successfully
```

### 3. Открыть в браузере

- **Frontend**: http://localhost:3000
- **Админка слотов**: http://localhost:3000/admin/slots
- **Админка провайдеров**: http://localhost:3000/admin/providers
- **Админка категорий**: http://localhost:3000/admin/categories

---

## 🧪 ПРОВЕРКА РАБОТЫ API

Из корня проекта выполните:

```bash
cd backend
node test-api-direct.js
```

Должны увидеть:

```
✅ /api/slots
✅ /api/providers
✅ /api/categories
✅ /api/themes
✅ /api/mechanics
```

---

## ⚠️ ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Backend не запускается:

1. Проверьте, что файл `backend/dev.db` существует
2. Проверьте `backend/.env` - должно быть:
   ```
   DATABASE_URL="file:./dev.db"
   ```
3. Остановите все Node процессы и попробуйте снова:
   ```bash
   # В PowerShell
   Get-Process -Name "node" | Stop-Process -Force
   ```

### Frontend не видит данные:

1. Убедитесь, что backend запущен (должен отвечать на http://localhost:3001)
2. Откройте консоль браузера (F12) → вкладка Network
3. Проверьте, что запросы идут на `localhost:3001/api/...`
4. Проверьте, что нет ошибок CORS или 404

### Проверить данные в базе:

```bash
cd backend
node check-data-simple.js
```

### Открыть Prisma Studio:

```bash
cd backend
npx prisma studio
```

Откроется на http://localhost:5555

---

## 📌 ПОЛЕЗНЫЕ КОМАНДЫ

Все команды из **КОРНЯ ПРОЕКТА**:

```bash
# Запустить backend
npm run backend

# Запустить frontend
npm run frontend

# Запустить оба одновременно (если есть такой скрипт)
npm run dev

# Остановить процессы - Ctrl+C в каждом терминале
```

---

## 💾 ВАЖНАЯ ИНФОРМАЦИЯ

- **База данных**: `backend/dev.db` (SQLite)
- **Порт Backend**: 3001
- **Порт Frontend**: 3000
- **Бэкапы**: `backups/db/` (на случай проблем)
- **Возврат к PostgreSQL**: используйте `backend/.env.supabase.backup`

---

## 🎯 БЫСТРЫЙ СТАРТ

1. Откройте 2 терминала Git Bash в папке `SlotQuest`
2. В первом: `npm run backend`
3. Во втором: `npm run frontend`
4. Откройте http://localhost:3000

✨ **Готово! Все данные должны отображаться!**
