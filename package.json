{
  "name": "slotquest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd backend && cross-env PORT=3001 npm run start\" \"cd frontend && cross-env NUXT_PORT=3000 npm run dev\"",
    "dev:watch": "concurrently \"cd backend && cross-env PORT=3001 npm run start:dev\" \"cd frontend && cross-env NUXT_PORT=3000 npm run dev\"",
    "studio": "cd backend && npx prisma studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@playwright/test": "^1.54.1",
    "concurrently": "^9.2.0",
    "cross-env": "^7.0.3",
    "dotenv": "^17.2.1"
  },
  "devDependencies": {
    "@iconify-json/heroicons": "^1.2.2"
  }
}
