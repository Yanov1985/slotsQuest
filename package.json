{
  "name": "slotquest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run start:dev --prefix backend\" \"cross-env NUXT_PORT=3000 npm run dev --prefix frontend\"",
    "studio": "cd backend && npx prisma studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@playwright/test": "^1.54.1",
    "concurrently": "^9.2.0",
    "cross-env": "^7.0.3"
  }
}
