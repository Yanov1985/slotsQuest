# 🚀 ФИНАЛЬНОЕ РУКОВОДСТВО ПО ЗАПУСКУ

## ✅ ЧТО УЖЕ ГОТОВО:

1. ✅ База данных SQLite создана (`backend/dev.db`)
2. ✅ Данные импортированы:
   - 94 провайдера
   - 6 механик
   - 241 тема
   - 31 категория
   - 5 слотов
3. ✅ Prisma Client сгенерирован
4. ✅ Все процессы остановлены

## 📝 ЧТО НУЖНО СДЕЛАТЬ:

### Шаг 1: Запустить Backend (В Git Bash)

```bash
cd backend
npm run start:dev
```

**Дождитесь сообщения:**

```
🚀 Backend server running on http://localhost:3001
```

### Шаг 2: Проверить API (В НОВОМ терминале Git Bash)

```bash
cd backend
node test-api-direct.js
```

**Ожидаемый результат:**

```
✅ /api/slots
✅ /api/providers
✅ /api/categories
✅ /api/themes
✅ /api/mechanics
```

### Шаг 3: Запустить Frontend (В НОВОМ терминале)

```bash
cd frontend
npm run dev
```

**Откройте в браузере:**

- Frontend: http://localhost:3000
- Админка: http://localhost:3000/admin/slots

---

## ⚠️ ЕСЛИ BACKEND НЕ ЗАПУСКАЕТСЯ:

1. Проверьте логи в терминале на наличие ошибок
2. Убедитесь, что файл `backend/dev.db` существует
3. Проверьте, что файл `backend/.env` содержит:
   ```
   DATABASE_URL="file:./dev.db"
   ```

## ⚠️ ЕСЛИ ДАННЫЕ НЕ ОТОБРАЖАЮТСЯ НА FRONTEND:

1. Откройте консоль браузера (F12)
2. Проверьте вкладку Network
3. Убедитесь, что запросы идут на `http://localhost:3001/api/...`
4. Проверьте, что нет ошибок CORS

---

## 🔧 БЫСТРЫЕ КОМАНДЫ:

### Проверить данные в базе:

```bash
cd backend
node check-data-simple.js
```

### Проверить API:

```bash
cd backend
node test-api-direct.js
```

### Открыть Prisma Studio:

```bash
cd backend
npx prisma studio
```

---

## 📌 ВАЖНО:

- Backend должен работать на порту **3001**
- Frontend должен работать на порту **3000**
- Все данные хранятся в `backend/dev.db`
- Для возврата к PostgreSQL используйте бэкапы в `backend/.env.supabase.backup`
