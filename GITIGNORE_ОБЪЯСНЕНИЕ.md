# 📁 ЧТО КОММИТИТСЯ В GIT, А ЧТО НЕТ

## ✅ ЧТО КОММИТИТСЯ (ОТСЛЕЖИВАЕТСЯ GIT)

### `backend/studio-data/` - JSON экспорты

```
backend/studio-data/
├── providers.json           ✅ Коммитится
├── mechanics.json           ✅ Коммитится
├── themes.json              ✅ Коммитится
├── slot_categories.json     ✅ Коммитится
├── slots.json               ✅ Коммитится
└── backup-summary.json      ✅ Коммитится
```

**Почему коммитятся:**

- ✅ Текстовые файлы (JSON)
- ✅ Легко отслеживать изменения
- ✅ Доступны на любом компьютере
- ✅ Можно восстановить данные после клонирования
- ✅ Git отлично работает с текстом

---

## ❌ ЧТО НЕ КОММИТИТСЯ (ИГНОРИРУЕТСЯ GIT)

### `backups/` - Бинарные .db файлы

```
backups/db/
├── dev_backup_LATEST.db          ❌ Игнорируется
├── dev_backup_2025-11-04.db      ❌ Игнорируется
└── dev_before_restore.db         ❌ Игнорируется
```

**Почему НЕ коммитятся:**

- ❌ Бинарные файлы (не текстовые)
- ❌ Большой размер (308 KB и больше)
- ❌ Git плохо работает с бинарными файлами
- ❌ Репозиторий раздуется
- ❌ Замедлит клонирование

**Добавлено в `.gitignore`:**

```gitignore
# Database backups (.db files should not be committed)
backups/
```

---

## 🎯 ДВОЙНАЯ СИСТЕМА БЭКАПОВ

### 1️⃣ Локальные .db файлы (НЕ в Git)

**Где:** `backups/db/`
**Для чего:** Мгновенное восстановление (2 сек)
**Хранятся:** Только на вашем компьютере

```bash
npm run restore  # ← Использует эти файлы
```

### 2️⃣ JSON экспорты (В Git)

**Где:** `backend/studio-data/`
**Для чего:** Облачное хранение, синхронизация
**Хранятся:** В Git, доступны везде

```bash
git clone ...
npm run db:restore-json  # ← Использует JSON из Git
```

---

## 🔥 WORKFLOW С GIT

### Ежедневный бэкап:

```bash
# 1. Создать полный бэкап
npm run backup

# Создаются:
# ✅ backups/db/*.db          (локально, не в Git)
# ✅ backend/studio-data/*.json  (в Git)

# 2. Закоммитить JSON в Git
git add backend/studio-data/
git commit -m "Daily backup"
git push

# ❌ backups/ НЕ попадет в коммит (игнорируется)
```

---

## 📊 СРАВНЕНИЕ

| Аспект                     | `.db` файлы           | JSON файлы             |
| -------------------------- | --------------------- | ---------------------- |
| **Местоположение**         | `backups/db/`         | `backend/studio-data/` |
| **Тип**                    | Бинарные              | Текстовые              |
| **Размер**                 | 308 KB                | 151 KB (все вместе)    |
| **В Git?**                 | ❌ Нет (игнорируется) | ✅ Да (коммитится)     |
| **Восстановление**         | 2 секунды             | 10 секунд              |
| **Доступность**            | Только локально       | На любом компьютере    |
| **Отслеживание изменений** | ❌ Невозможно         | ✅ Легко               |

---

## 🎓 АНАЛОГИЯ С ИНТЕРНЕТ-МАГАЗИНОМ

**Представьте склад с товарами:**

### .db файлы = **Фотография склада**

- Быстрая (1 сек сделать)
- Мгновенно восстановить (2 сек)
- Но нельзя отправить по email (бинарная, большая)
- Хранится локально в офисе

### JSON файлы = **Опись товаров**

- Текстовый документ
- Можно отправить коллегам (Git)
- Можно читать и редактировать
- Доступна в облаке

**Если офис сгорит:**

- ❌ Фото склада потеряно (локальные .db)
- ✅ Опись в облаке цела (JSON в Git)

---

## 💡 РЕКОМЕНДАЦИИ

### ✅ ДЕЛАЙТЕ:

1. **Коммитьте JSON после каждого изменения данных:**

   ```bash
   npm run backup
   git add backend/studio-data/
   git commit -m "Added new slots"
   git push
   ```

2. **Локальные .db используйте для быстрого отката:**
   ```bash
   npm run backup:quick  # Перед экспериментами
   # ... что-то сломалось
   npm run restore       # Быстрый откат
   ```

### ❌ НЕ ДЕЛАЙТЕ:

1. ❌ Не удаляйте `backups/` из `.gitignore`
2. ❌ Не коммитьте .db файлы вручную
3. ❌ Не храните только .db без JSON

---

## 🚀 ПРИ КЛОНИРОВАНИИ НА НОВЫЙ КОМПЬЮТЕР

```bash
# 1. Клонировать репозиторий
git clone https://...
cd SlotQuest

# 2. Установить зависимости
npm install
cd backend && npm install

# 3. Восстановить данные из JSON (которые в Git)
cd backend
npm run db:restore-json

# 4. Создать локальный .db бэкап
npm run db:backup

# Готово! Теперь есть и локальные .db, и JSON
```

---

## ✅ ИТОГО

### Что исправлено:

✅ Добавлен `backups/` в `.gitignore`
✅ Git теперь игнорирует .db файлы
✅ JSON файлы по-прежнему отслеживаются
✅ Двойная система защиты работает правильно

### Что происходит сейчас:

```bash
git status

# Видно:
# ✅ backend/studio-data/*.json  (отслеживается)

# НЕ видно:
# ❌ backups/db/*.db  (игнорируется)
```

---

✨ **Теперь у вас правильная настройка Git для работы с бэкапами!** ✨

**Локальные .db** - для скорости
**JSON в Git** - для надежности


