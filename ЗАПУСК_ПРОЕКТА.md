# 🚀 ИНСТРУКЦИЯ ПО ЗАПУСКУ SLOTQUEST

## ✅ СТАТУС: ВСЕ ДАННЫЕ ВОССТАНОВЛЕНЫ!

📊 **В базе данных:**

- ✅ 94 провайдера
- ✅ 6 механик
- ✅ 241 тема
- ✅ 31 категория
- ✅ 5 слотов

💾 **База данных:** `backend/dev.db` (SQLite)

---

## 🚀 ПОШАГОВЫЙ ЗАПУСК

### Шаг 1: Остановите все процессы

В PowerShell выполните:

```powershell
Get-Process -Name "node" -ErrorAction SilentlyContinue | Stop-Process -Force
```

### Шаг 2: Откройте 2 терминала Git Bash

**Оба терминала должны быть открыты в корневой папке проекта:**

```
D:\www\Projects\2025\SlotQuest
```

### Шаг 3: Запустите Backend

**В ПЕРВОМ терминале Git Bash:**

```bash
npm run dev:watch
```

Или альтернативно:

```bash
cd backend
npm run start:dev
```

**Дождитесь сообщения:**

```
🚀 Backend server running on http://localhost:3001
📊 API available at http://localhost:3001/api
```

### Шаг 4: Проверьте API (опционально)

**Во ВТОРОМ терминале (или новом):**

```bash
cd backend
node test-api-direct.js
```

**Должны увидеть:**

```
✅ /api/slots
✅ /api/providers
✅ /api/categories
✅ /api/themes
✅ /api/mechanics
```

### Шаг 5: Откройте в браузере

- **Frontend**: http://localhost:3000
- **Админка слотов**: http://localhost:3000/admin/slots
- **Админка провайдеров**: http://localhost:3000/admin/providers

---

## 🔍 ДИАГНОСТИКА ПРОБЛЕМ

### Если Backend не запускается:

1. **Проверьте файл `.env` в папке `backend`:**

   ```bash
   cd backend
   cat .env
   ```

   Должно быть:

   ```
   DATABASE_URL="file:./dev.db"
   DIRECT_URL="file:./dev.db"
   ```

2. **Проверьте, что база данных существует:**

   ```bash
   cd backend
   ls -la dev.db
   ```

3. **Проверьте данные в базе:**
   ```bash
   cd backend
   node check-data-simple.js
   ```

### Если Frontend не видит данные:

1. **Откройте консоль браузера (F12)**
2. **Перейдите на вкладку Network**
3. **Обновите страницу**
4. **Проверьте запросы:**

   - Должны идти на `localhost:3001/api/...`
   - Статус должен быть 200 (не 404, не 500)

5. **Проверьте вкладку Console на наличие ошибок**

---

## 🧪 ПОЛЕЗНЫЕ КОМАНДЫ

### Проверить данные в базе:

```bash
cd backend
node check-data-simple.js
```

**Ожидаемый результат:**

```
🏢 Провайдеры:     94
🔧 Механики:       6
🎨 Темы:           241
📁 Категории:      31
🎰 Слоты:          5
```

### Проверить API:

```bash
cd backend
node test-api-direct.js
```

### Открыть Prisma Studio:

```bash
cd backend
npx prisma studio
```

Откроется на http://localhost:5555

### Пересоздать Prisma Client:

```bash
cd backend
npx prisma generate
```

---

## ⚠️ ЧАСТЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### 1. "Cannot find module '@prisma/client'"

**Решение:**

```bash
cd backend
npx prisma generate
npm install
```

### 2. Backend запущен, но API не отвечает

**Проверка:**

```bash
# В браузере откройте:
http://localhost:3001/api/slots
```

Если видите JSON с данными - все работает!

### 3. База данных пустая

**Восстановление:**

```bash
cd backend
node import-from-backup-sql.js
```

### 4. Frontend показывает пустую страницу

**Проверьте:**

1. Backend запущен на порту 3001
2. В консоли браузера нет ошибок
3. API отвечает (откройте http://localhost:3001/api/slots)

---

## 📌 ВАЖНАЯ ИНФОРМАЦИЯ

- **Backend порт:** 3001
- **Frontend порт:** 3000
- **База данных:** `backend/dev.db` (SQLite)
- **Бэкапы:** `backups/db/`
- **Конфигурация:** `backend/.env`

---

## 🎯 ФИНАЛЬНЫЙ ЧЕКЛИСТ

- [ ] Остановлены все Node процессы
- [ ] Открыты 2 терминала Git Bash в корне проекта
- [ ] В первом терминале запущен: `npm run dev:watch`
- [ ] Backend показывает: "Backend server running"
- [ ] API test показывает: все endpoints ✅
- [ ] Открыт браузер на http://localhost:3000
- [ ] В админке видны данные

---

## 💡 СОВЕТ

Если что-то не работает:

1. **Остановите ВСЕ** процессы Node
2. **Закройте ВСЕ** терминалы
3. **Откройте НОВЫЕ** терминалы
4. **Следуйте инструкции** с начала

---

✨ **Удачи! Все данные готовы к работе!** ✨
