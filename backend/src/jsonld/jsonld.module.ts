/**
 * 📦 JSON-LD Module - Модуль для работы с JSON-LD схемами
 *
 * Модуль в NestJS - это как "отдел" в компании.
 * Он объединяет связанные компоненты (сервис, контроллер, DTO)
 * и делает их доступными для остального приложения.
 *
 * Этот модуль отвечает за:
 * - Генерацию JSON-LD схем для SEO
 * - Валидацию структурированных данных
 * - API эндпоинты для работы с JSON-LD
 */

import { Module } from '@nestjs/common';
import { JsonLdController } from './jsonld.controller';
import { JsonLdService } from './jsonld.service';
import { PrismaModule } from '../prisma/prisma.module';

@Module({
  // 📥 Импорты - модули, от которых зависит этот модуль
  imports: [
    PrismaModule, // Для работы с базой данных через Prisma
  ],

  // 🎮 Контроллеры - обработчики HTTP запросов
  controllers: [
    JsonLdController, // API эндпоинты для JSON-LD
  ],

  // 🔧 Провайдеры - сервисы с бизнес-логикой
  providers: [
    JsonLdService, // Сервис генерации и валидации JSON-LD
  ],

  // 📤 Экспорты - что будет доступно другим модулям
  exports: [
    JsonLdService, // Экспортируем сервис для использования в других модулях (например, SlotsModule)
  ],
})
export class JsonLdModule {}
